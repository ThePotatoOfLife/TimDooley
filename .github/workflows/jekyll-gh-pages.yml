name: Legacy Jekyll workflow (disabled)

# The repository uses .github/workflows/pages.yml for deployment.
# This file is retained as documentation only and no longer runs automatically.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled; use pages.yml."
