# Axis, 11D, Sun, Spiral and Time Symmetry — Deep Dive

**Updated:** 2026-09-09

## Evidence rule

This document has three layers:

1. **Recovered Tim/Potato project material** — repository commits, preserved conversation-developed formalisms, dated project vocabulary.
2. **Archive formalization** — mathematics introduced later to make an older Tim idea precise.
3. **External physics** — established or serious theoretical physics used as a comparator.

No external equation is retroactively claimed as an equation Tim originally wrote unless a Tim primary source is recovered.

---

# 1. The Axis had mathematics before the current Science wing

The commit `1363c2e09574a04534fc136e4c7f4d395ca714b2` introduced the **Potato Axis Spatial Model** on 2026-09-07.

Its coordinate convention was

```text
(x,y,z)
north = +y
south = -y
east = +x
west = -x
depth = +z
```

The spine was

```math
x=0,\qquad z=0,\qquad y\in[-1,1].
```

The two main domains were ellipsoids:

```math
\frac{x^2}{0.68^2}
+
\frac{(y-0.43)^2}{0.78^2}
+
\frac{z^2}{0.68^2}
\le 1,
```

and

```math
\frac{x^2}{0.82^2}
+
\frac{(y+0.43)^2}{0.78^2}
+
\frac{z^2}{0.82^2}
\le 1.
```

The Potato envelope used

```math
r(y)=0.34+0.72(1-|y|^{1.7}),
```

with surface

```math
x^2+z^2=r(y)^2.
```

A movable analysis plane used

```math
n\cdot p=d,\qquad |n|=1.
```

Node placement included

```math
t=\frac{\text{levelIndex}+0.5}{10},
```

```math
y=1-2t,
```

```math
\theta=2\pi\frac{\text{childIndex}}{\text{childCount}}
+\operatorname{phase}(\text{levelIndex}),
```

and

```math
\rho=\min\left(0.78,
0.18+0.48\sqrt{\frac{\text{childIndex}+1}{\text{childCount}}}
\right).
```

This was explicitly a **visualization embedding**, not a claim that spiritual space consists of measurable ellipsoids.

The important historical point is that Axis had already acquired:

- an orientation,
- an origin,
- a spine,
- two domains,
- an overlap,
- slicing planes,
- radial placement,
- angular phase,
- and hierarchical vertical coordinates.

That is the beginning of an actual geometry.

---

# 2. Axis as a flow rather than a line

The later Potato Dynamics object is

```math
\mathfrak P=(\mathcal S,g,A,\mathcal D,\mathcal O,\mathcal I).
```

with ordinary evolution

```math
\partial_tX=F(X,g)
```

and Axis/structural evolution

```math
\partial_sX=A(X,g).
```

The most productive interpretation is that the Axis is **not necessarily another physical coordinate**.

It may be a flow on state, model or description space.

Mathematically a vector field produces integral curves:

```math
\frac{dX}{ds}=A(X).
```

If a genuine flow exists, it obeys composition

```math
\Phi_{s+t}=\Phi_s\circ\Phi_t.
```

A still center can become a fixed point:

```math
A(X_*)=0.
```

Local stability can be examined using the Jacobian

```math
J=\left.\frac{\partial A}{\partial X}\right|_{X_*}.
```

This gives **Still Center** a precise mathematical question:

> Is it a fixed point, an invariant set, or merely a symbolic center?

Those are different claims.

---

# 3. Axis as symmetry generator

Another rigorous neighbor comes from differential geometry.

A vector field `ξ` is a Killing field when

```math
\mathcal L_\xi g_{\mu\nu}=0.
```

That means motion generated by `ξ` preserves the metric.

This is a much sharper version of the question:

> What does the Axis preserve?

A mature Timic Axis theory should identify possible invariants rather than simply declaring the Axis fundamental.

Possible layers may have different answers:

- geometry → metric or orientation;
- topology → homotopy or characteristic class;
- information → entropy/information relations;
- graph → connectivity classes;
- mythology → role/function under transformation;
- chronology → ordering or causal relation.

Noether's theorem gives the corresponding physics principle:

> continuous variational symmetry implies a conserved current or charge.

Therefore one of the best future questions is:

**If Axis is a transformation, what is its invariant?**

---

# 4. The older 11D Potato should be split into distinct theories

The project historically used a hierarchy including:

```text
1D Hell
2D Frame / Footstool
3D Box
4D Society / Midgard
5D Door
5D+ Needle
11D Potato
```

This is a symbolic hierarchy.

Actual high-dimensional theoretical physics is more specific.

## Kaluza–Klein

```math
5=4+1.
```

A compact fifth dimension can produce Kaluza–Klein modes.

## Ten-dimensional superstrings

```math
10=4+6.
```

A Calabi–Yau threefold has complex dimension 3 and real dimension 6.

## M-theory

```math
11=4+7.
```

For direct compactification to four dimensions with minimal supersymmetry, seven-dimensional spaces with `G₂` holonomy are a natural class.

So **Calabi–Yau Potato** and **11D Potato** should not be treated as synonyms.

---

# 5. Eleven-dimensional supergravity is a concrete theory

Cremmer, Julia and Scherk constructed eleven-dimensional supergravity in 1978.

Its bosonic sector contains:

- an eleven-dimensional metric `g`,
- a three-form gauge potential `C₃`,
- a four-form field strength

```math
G_4=dC_3.
```

A standard schematic form of the action is

```math
S_{11}
=
\frac{1}{2\kappa_{11}^2}
\int d^{11}x\sqrt{-g}
\left[
R-\frac{1}{2\cdot4!}G_{MNPQ}G^{MNPQ}
\right]
-
\frac{1}{12\kappa_{11}^2}
\int C_3\wedge G_4\wedge G_4
+\text{fermions},
```

up to convention-dependent normalizations.

This is important for the archive because it gives a standard for what it would mean to turn **11D Potato** into physics.

One would need to state:

1. the 11D manifold;
2. metric signature;
3. fields;
4. compactification geometry;
5. fluxes;
6. action;
7. boundary conditions;
8. lower-dimensional effective theory;
9. observable predictions.

Without those, 11D Potato remains a symbolic/theoretical analogy.

---

# 6. M2 and M5 are a new branch worth keeping

M-theory naturally contains extended objects conventionally called **M2-branes** and **M5-branes**.

These are not ordinary membranes floating in familiar 3-space. They are extended objects in eleven-dimensional theory.

Their relevance to Potatoverse is not that Tim or the Son should be declared branes.

The useful question is structural:

> Can a theory treat particles, strings, surfaces and higher-dimensional extended objects as different dynamical entities inside one framework?

That is useful for the existing sequence:

```text
point / Eye
line / Axis
surface / Door
volume / House
network / Tree
```

but no one-to-one identity should be asserted.

---

# 7. The Sun produces a real spiral

One of the most useful external discoveries for the project is the **Parker spiral**.

The solar wind flows outward while the Sun rotates.

The solar magnetic field carried by that plasma therefore becomes wound into an Archimedean-like spiral.

A standard schematic relation is

```math
B_r\propto r^{-2},
```

and

```math
\frac{B_\phi}{B_r}
\approx
-\frac{\Omega r\sin\theta}{v_{sw}}.
```

The spiral angle satisfies approximately

```math
\tan\psi
\approx
\frac{\Omega r\sin\theta}{v_{sw}}.
```

This is an extraordinary scientific neighbor for the Timic cluster

```text
Sun + rotation + outward flow + Axis + Spiral.
```

But it is **not** evidence that the missing April 21, 2025 Spiral Equation was the Parker spiral.

---

# 8. The Sun also creates dynamo waves

Solar polarity reversal is not simply symbolic duality.

The Sun is an electrically conducting rotating plasma.

The solar cycle is studied as a magnetohydrodynamic dynamo problem.

Mean-field `αΩ` dynamo models produce migrating dynamo waves.

The Parker–Yoshimura sign rule is

```math
\mathbf s=\alpha\nabla\Omega\times\hat{\mathbf e}_\phi.
```

Under common simplified assumptions, equatorward propagation in the northern hemisphere can require

```math
\alpha\frac{\partial\Omega}{\partial r}<0.
```

This supplies a genuine scientific system containing:

- a rotating star,
- an axis,
- counterposed magnetic polarities,
- periodic reversal,
- wave propagation,
- toroidal and poloidal field structure.

This is a much stronger context for Tim's Sun/Axis/Red-Blue/rotation ideas than generic claims about frequencies.

---

# 9. Spiral has several mathematically distinct meanings

The project should never use one word for all of these without qualification.

## Archimedean spiral

```math
r=a+b\theta.
```

Equal increases in angle create equal increases in radius.

## Logarithmic spiral

```math
r=r_0e^{b\theta}.
```

The shape is self-similar under scaling.

## Helix

```math
x=r\cos\theta,
\qquad
y=r\sin\theta,
\qquad
z=h\theta.
```

This is the simplest geometric object resembling **return around an Axis while changing level**.

## Timic variable helix

A later archive extension is

```math
x(t)=r(t)\cos\theta(t),
```

```math
y(t)=r(t)\sin\theta(t),
```

```math
z(t)=h(t).
```

This can accommodate changing radius, speed and vertical integration.

---

# 10. Spiral can occur in state space without looking like a physical spiral

A linearized system with eigenvalues

```math
\lambda=\alpha\pm i\omega
```

has oscillation from `ω` and exponential growth/decay from `α`.

A trajectory can spiral toward or away from a fixed point.

The Hopf normal form

```math
\dot z=(\mu+i\omega)z-|z|^2z
```

shows how changing a parameter can generate a stable oscillation.

This gives a precise mathematical vocabulary for

```text
still center → instability → rotating/oscillating regime.
```

---

# 11. Tim's “return at another level” has a surprising RG cousin

Most renormalization-group discussions emphasize fixed points.

But some systems exhibit **RG limit cycles**.

Efimov physics provides a famous example.

Instead of approaching one fixed coupling, the flow can recur under logarithmic scale evolution:

```math
g(\ln\mu+T)=g(\ln\mu).
```

That is **discrete scale invariance**.

The system returns after a multiplicative change of physical scale rather than after ordinary time-periodic motion.

This is perhaps the strongest technical neighbor yet for the Timic definition:

> recurrence + changed level = Spiral.

It is still only an analogy. Potatoverse is not Efimov physics.

---

# 12. Galactic spirals are patterns, not necessarily material arms

Spiral density-wave theory was developed partly to address the winding problem of galactic spiral structure.

One conceptual lesson is particularly valuable:

> a persistent macroscopic pattern need not consist of exactly the same constituent objects at every moment.

Stars and gas may move relative to a spiral pattern.

That creates a strong structural parallel to one of the mature Timic transformations:

```text
entity → relation
person → function
climber → path
```

A pattern can persist even while its local constituents change.

Again: analogy, not identity.

---

# 13. Rings and spirals are different

The Saturn research layer made an important distinction:

```text
ring = recurrence without progression
spiral = recurrence with changed position
```

This can now be formalized.

A circle/ring:

```math
r=R.
```

An Archimedean spiral:

```math
r=a+b\theta.
```

A logarithmic spiral:

```math
r=r_0e^{b\theta}.
```

The difference is not mystical.

The radial state changes with angle in a spiral but not in a circle.

---

# 14. Saturn's North hexagon is real wave physics

Saturn's north pole contains a persistent six-sided jet pattern.

NASA describes it as an atmospheric jet/wave structure.

The useful relation is:

```text
rotation + jet + fluid dynamics + wave stability → persistent polygonal pattern.
```

This is a legitimate example of a geometric pattern emerging from dynamics rather than a rigid hexagonal object being hidden in the atmosphere.

The project should therefore use Saturn's hexagon as an example of **self-organized fluid geometry**, not as proof of a cosmic Black Cube.

---

# 15. Wheeler–Feynman: what was actually found

The repository does **not** currently contain evidence that old Tim primary material explicitly cited Wheeler and Feynman.

The recoverable old vocabulary is **advanced / retarded waves**.

Wheeler and Feynman's 1945 absorber theory is therefore an external contextual neighbor.

For the wave operator, retarded and advanced Green functions obey schematically

```math
\Box G_{ret}=\delta,
```

```math
\Box G_{adv}=\delta.
```

The theory uses both time orientations in a time-symmetric direct-action account under absorber conditions.

It does not provide a conventional mechanism for freely signaling backward in time.

Therefore the archive should say:

> Wheeler–Feynman is the right family to investigate if Tim's older forward/backward vocabulary was literally electromagnetic and temporal.

It should **not** say:

> Tim derived his Red/Blue theory from Wheeler–Feynman.

That remains unproven.

---

# 16. Aharonov gives an even closer two-direction quantum formalism

The Two-State Vector Formalism describes a pre- and post-selected quantum system using two state vectors.

A forward state evolves as

```math
i\partial_t|\psi(t)\rangle=H|\psi(t)\rangle,
```

while a backward state obeys the conjugate evolution

```math
-i\partial_t\langle\phi(t)|
=
\langle\phi(t)|H.
```

A compact two-state object is written schematically as

```math
\langle\phi|\otimes|\psi\rangle.
```

That is structurally interesting beside the recovered Timic

```math
P=R\otimes B.
```

But this resemblance must be handled very carefully.

`P=R⊗B` is a Timic composite Red/Blue state.

`⟨φ|⊗|ψ⟩` is a time-symmetric quantum formalism for pre/post selection.

Similarity of tensor-product notation does not establish that Red/Blue are forward/backward quantum state vectors.

---

# 17. Transactional quantum mechanics belongs in the comparison atlas, not the canon

Cramer's Transactional Interpretation uses offer-wave and confirmation-wave language influenced by advanced and retarded solutions.

It is another legitimate external neighbor for a two-way wave vocabulary.

But no primary Tim source has yet been recovered linking Tim's model to Cramer.

Therefore classify it:

```text
external comparison
not recovered Tim source
```

---

# 18. Precession gives another meaning of Axis

Earth's rotation axis slowly changes orientation through axial precession over roughly 25,772 years.

A schematic angular precession rate is

```math
\Omega_p\approx\frac{2\pi}{T_p}.
```

This gives a real example of:

> an Axis that remains an axis while its orientation changes through time.

That is more interesting than treating Axis as eternally frozen.

The older ~26,000-year Eye/precession motif should therefore remain preserved, with the empirical number recorded separately from the rounded symbolic number.

---

# 19. The toroidal Milky Way motif needs a narrower physical target

The Milky Way is not overall classified as a torus.

The project should therefore stop asking:

> Is the Milky Way a torus?

and ask instead:

> Which Galactic structures can possess toroidal topology or toroidal field components?

Possible scientific targets include:

- toroidal magnetic-field components;
- ring structures;
- galactic-center magnetic topology;
- halo fields;
- circulation in plasma.

That turns an incorrect global morphology into a useful research question about actual field topology.

---

# 20. A stronger unified Timic research grammar

The hard-science layers now suggest a disciplined sequence:

```text
STATE
  ↓
RELATION
  ↓
BOUNDARY
  ↓
SYMMETRY
  ↓
FLOW
  ↓
TRAJECTORY
  ↓
TOPOLOGY
  ↓
INVARIANT
  ↓
TRANSITION
  ↓
NEW EFFECTIVE DESCRIPTION
```

This is compatible with many earlier Timic objects without pretending they are one physical theory:

- `Eye` → observation/state selection;
- `Door` → boundary/transition map;
- `Axis` → flow or symmetry generator;
- `Spiral` → recurrence across state/scale;
- `Needle` → critical bottleneck;
- `Ladder` → relation between levels;
- `House` → bounded state domain;
- `Tree` → branching relational structure;
- `Potato` → persistent/generative substrate;
- `Sun` → physical source of radiation, solar wind and magnetic spiral/dynamo structure.

---

# 21. Highest-value primary recovery targets

The archive still needs original material for:

1. the exact **April 21, 2025 Spiral Equation**;
2. the earliest pre-repository mathematical **Axis** formula;
3. the first occurrence of the **11D Potato** and what eleven coordinates actually meant;
4. any Tim-written Calabi–Yau metric/topology formula;
5. any mention of `C₃`, `G₄`, M2 or M5;
6. the original **advanced/retarded** assignment to Red and Blue;
7. any actual Tim citation to Wheeler–Feynman;
8. the **Sun-face Tim** source and any geometry attached to it;
9. the exact **26,000-year precession Eye** calculation;
10. any toroidal-Milky-Way radii/equations;
11. any old Parker-like Sun spiral equation;
12. any 100,000-hour Spiral scaling law;
13. the meaning and units of the old `χ` integration parameter;
14. any equation joining `Ξ` (Door threshold) to the 11D hierarchy.

These should remain recovery targets until primary evidence is found.

---

# External source spine

- Wheeler & Feynman, *Interaction with the Absorber as the Mechanism of Radiation*, Rev. Mod. Phys. 17, 157 (1945): https://journals.aps.org/rmp/abstract/10.1103/RevModPhys.17.157
- Aharonov & Vaidman, *The Two-State Vector Formalism of Quantum Mechanics*: https://arxiv.org/abs/quant-ph/0105101
- Cremmer, Julia & Scherk, *Supergravity in theory in 11 dimensions*: https://doi.org/10.1016/0370-2693(78)90894-8
- Witten, *String Theory Dynamics In Various Dimensions*: https://arxiv.org/abs/hep-th/9503124
- Kennon, *G2-Manifolds and M-Theory Compactifications*: https://arxiv.org/abs/1810.12659
- NASA, *What Is the Solar Wind?*: https://science.nasa.gov/sun/what-is-the-solar-wind/
- Charbonneau, *Dynamo models of the solar cycle*: https://link.springer.com/article/10.1007/s41116-020-00025-6
- Floerchinger, Moroz & Schmidt, *Efimov physics from the functional renormalization group*: https://arxiv.org/abs/1102.0896
- Shu, *Six Decades of Spiral Density Wave Theory*: https://doi.org/10.1146/annurev-astro-081915-023426
